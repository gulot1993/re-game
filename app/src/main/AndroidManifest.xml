<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.regame">
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Regame">
        <activity android:name=".Features.MainGame"/>
        <activity android:name=".Features.Video"/>
        <activity android:name=".Features.Story"/>
        <activity android:name=".Features.Leaderboard.GameListActivity"/>
<!--        <activity android:name=".Features.signup.SignUpActivity" />-->
<!--        <activity android:name=".Features.signin.SignInActivity" />-->
        <activity android:name=".Features.signin.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>